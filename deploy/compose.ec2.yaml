version: "3.9"

services:
  app:
    image: ${IMAGE_URI}
    container_name: marketing-app
    restart: always

    ports:
      - "${HOST_PORT}:8000"

    env_file:
      - .env

    environment:
      GROQ_API_KEY: ${GROQ_API_KEY}
      HOST: 0.0.0.0
      PORT: 8000